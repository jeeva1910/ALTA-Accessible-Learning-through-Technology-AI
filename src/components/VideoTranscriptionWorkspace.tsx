import {
  MediaTranscriptionWorkspace,
  TranscriptSegment,
  LectureSample,
  WorkspaceSourceMode,
  MediaType
} from './MediaTranscriptionWorkspace';

export type { TranscriptSegment, LectureSample, WorkspaceSourceMode, MediaType };

export const VideoTranscriptionWorkspace = MediaTranscriptionWorkspace;

export default MediaTranscriptionWorkspace;
